require '_h2ph_pre.ph';

no warnings qw(redefine misc);

unless(defined(&__der_protos_h__)) {
    eval 'sub __der_protos_h__ () {1;}' unless defined(&__der_protos_h__);
    require 'stdarg.ph';
    if(defined(&__cplusplus)) {
    }
    if(defined(&__cplusplus)) {
    }
}
1;
